import { ImportedVariableSet } from './resolveRequired';
export default function recusiveAdaptExportsToIEV(pathResolver: (path: string, originalDirNameOverride?: string) => string, varToFilePath: ImportedVariableSet, validExtends: (fullFilePath: string) => boolean): Promise<void>;
/**
 * Resolves specified variables to their actual files
 * Useful when using intermeriary files like this
 *
 * ```js
 * export mixin from "path/to/mixin"
 * ```
 *
 * @param pathResolver
 * @param varToFilePath
 */
export declare function adaptExportsToIEV(pathResolver: (path: string, originalDirNameOverride?: string) => string, varToFilePath: ImportedVariableSet, validExtends: (fullFilePath: string) => boolean): Promise<void>;
